# 벽 검출 정밀화 — 실측 현황과 남은 것

대상 `dxf_parser.py`. 측정 도면 `지하3층 건축평면도.dxf`.
원칙: 각 항목은 독립 적용·독립 검증·독립 롤백. 좌표 100% 결정론 유지.

> 이 문서의 2026-06 판은 **전제가 낡아 있었다.** 그때 지적한 "쌍별 그리디 1:1"
> 페어링은 이후 interval-greedy 로 재작성됐고(`_find_wall_pairs` 독스트링),
> 지시서를 그대로 실행했다면 이미 해결된 문제를 다시 고쳤을 것이다.
> 아래는 2026-09 재측정 결과다. **숫자 없이 항목을 되살리지 말 것.**

## 기준선 재측정 (2026-09)

```bash
python -c "import dxf_parser as P,collections; r=P.load_layer_map('layer_map.csv'); b=P.load_layer_map('block_map.csv'); d=P.parse(r'...지하3층 건축평면도.dxf',r,b); W=d['elements']['wall']; print(collections.Counter(w.get('pairing') for w in W), len(W))"
```

| | 2026-06 (지시서 작성 시) | 2026-09 (현재) |
|---|---|---|
| paired | 383 | **441** |
| single_offset | 221 | **158** |
| closed | 37 | 38 |
| 면선 커버리지 | — | **95%** |

---

## 완료 / 폐기

**2단계(핵심) — 라인-버킷 페어링: 폐기.** interval-greedy 로 이미 해결됐다.
긴 면선 1개가 구간별로 여러 파트너와 매칭되므로 단편화가 더는 탈락 사유가 아니다.
지시서가 근거로 든 "894개 중 671개가 `overlap<0.3` 탈락"은 현재 코드에 해당하지 않는다.

**1단계 — 적응형 갭 병합: 측정 후 기각.** `_SEG_MERGE_GAP_MM` 을 올려봐도 효과가 없다.

| gap | paired | single_offset | 커버리지 |
|---|---|---|---|
| 50 (현행) | 441 | 158 | 95% |
| 450 (지시서 제안) | 443 | 150 | 95% |
| 700 | 434 | 127 | 95% |

벽 8개를 얻자고 450mm 개구부를 가로지를 위험을 지지 않는다. 700 은 오히려 paired 가 준다.
2단계가 이미 단편화를 흡수하므로 입력단 병합이 더 줄 게 없다 — 원인이 사라진 것이다.

---

## 남은 것

### A. 가짜 얇은 쌍이 정답 쌍을 선점한다 ★ 코드 아님, 매핑 문제

`paired` 중 두께 60mm 미만이 83개다.

| 레이어 | 개수 | 실제 정체 |
|---|---|---|
| A-CON | 31 | 콘크리트 벽이 50mm 일 수 없다 — 마감선/모따기선과 오결합 |
| A-STEEL | 27 | 밀착 철골(정상) |
| 상부골조 | 19 | 오결합 |
| A-INSUL | 4 | 단열재(정상) |

`_find_wall_pairs` 는 수직거리 오름차순으로 구간을 선점한다. 그래서 **50mm 짜리
가짜 쌍이 먼저 구간을 먹고 200/450mm 정답 쌍을 막는다.**

`opts: pair_min` 으로 그 레이어의 하한을 올리면 정답이 통과한다(실측):

```
^(A-CON|상부골조)$,wall,200,2800,,pair_min=80
```

| | 두께 <60mm | 두께 분포 |
|---|---|---|
| 현행 | 83 | 200×156, 250×120, **50×55**, 400×33, 30×24 |
| pair_min=80 | **30** | 200×156, 250×124, **450×41**, 400×33, 30×24 |

50mm 벽 55개가 450/250mm 로 정정된다 — 물량에 직접 영향이다.
**전역 상수로 올리지 않는다**: `WALL_PAIR_MIN_MM=1.0` 은 A-STEEL 을 위해 일부러 낮춘 값이고,
실제로 50~100mm 경량벽체는 존재한다. 도면마다 판단할 일이라 layer_map 에 적는다.

### B. 3단계 collinear 갭 축소 — 측정 완료, 조치 없음

`_merge_two_segments` 가 `seg_length` 를 갱신하지 않아 계측이 불가능했다. 고친 뒤 재측정:

| COLLINEAR_GAP | 벽 수 | 총연장 |
|---|---|---|
| 500 (현행) | 637 | 1297.0 m |
| 250 | 642 | 1295.8 m |
| 120 (지시서 제안) | 675 | 1288.4 m |

500→120 의 차이는 **8.6m / 1297m = 0.7%** 다. 갭 38개를 평균 226mm 씩 잇고 있다는
뜻인데, 그 크기는 교차벽·기둥 자리이지 개구부가 아니다(문은 800mm+, 그보다 좁은
개구부는 `opening` 레코드로 따로 잡힌다). **0.7% 를 위해 상수를 건드리지 않는다.**

### C. 4단계 곡선벽(ARC) — 완료(진단은 지시서와 달랐다)

지시서는 "평행판정 실패 → 누락" 이라 했지만 실측하니 **호는 이미 페어링된다**
(코드 각도 차 0.4°, 허용 5°). 진짜 문제는 **두께가 틀리게 측정되는 것**이었다.

`ARC_SEG_PER_RAD=8` 은 고정 **각도** 밀도라 코드 근사 오차(사지타)가 반지름에 비례한다.

| 반지름 | 사지타(수정 전) | 측정 두께 |
|---|---|---|
| 6,000 | 12mm | 289~305 (참값 300) |
| 13,040 | 27mm | **476~478 (참값 500)** — 4.8% 오차 |

두께를 mm 로 재는 파이프라인에서 근사 오차만 각도로 두면 큰 곡선벽에서 반드시
틀어진다. 반지름이 커질수록 **조용히** 더 틀린다.

수정: `_arc_segments(r, span)` — 사지타를 mm 로 묶는다(`ARC_CHORD_TOL_MM=5`,
`s = r(1-cos(δ/2)) ≤ tol → δ ≤ 2·acos(1-tol/r)`). `arc_to_points` 와
LWPOLYLINE bulge(`_bulge_points`) 둘 다 이걸 쓴다 — 같은 버그였다.

결과: 두께 476→495~497, 299~301. 곡선벽 회수 15.7→16.7m.
비용: 벽 레코드 640→678(+6%), 빌드 52s→58s. `ARC_MAX_SEGS=128` 로 폭주 방지.
`tol=1` 도 시험했다(두께 499~500, 레코드 766). 1mm 는 제도 오차 이하라 5mm 로 둔다.

### D. single_offset 158개 — 진단 완료, 원인은 A/B 와 같은 두 레이어

"115개가 레이어 빈 문자열" 은 `_merge_two_segments` 가 병합 레코드를 새로 만들면서
`layer` 를 떨어뜨린 것이었다(수정 완료). 실제 분포:

    A-CON 87 · 상부골조 68 · A-WALL 2 · Con 1     (빈 레이어 0)

짝을 못 찾는 면선이 **A 항목의 가짜 얇은 쌍과 정확히 같은 두 레이어**에 몰려 있다.
그 레이어들이 벽면 옆에 마감/보조선을 함께 그린 도면이라는 뜻이다.
다음 수순은 알고리즘이 아니라 그 레이어의 선 구성을 눈으로 확인하는 것
(`python preview.py <json>` 에서 single_offset 색으로 구분된다).
