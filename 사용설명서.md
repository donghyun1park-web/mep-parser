# MEP-Parser 사용 설명서

DXF 건축/설비 평면도를 **100% 결정론적**으로 분석해 3D 모델(IFC / FreeCAD)로 빌드하는 도구.
벽은 외곽선(두 평행 면선)으로 그려진 도면을 자동으로 **센터선 + 두께**로 변환해 세웁니다.

---

## 1. 개요

```
 DXF 도면 ──> dxf_parser.py ──> geometry.json ──┬──> ifc_builder.py  ──> .ifc   (FreeCAD 불필요)
 (외곽선)     (결정론 추출)      (단일 계약 파일) ├──> freecad_builder ──> .FCStd + .ifc
                                                 └──> preview.py     ──> .html  (브라우저 3D)
```

- **단일 계약 = `geometry.json`**. 파싱 결과는 전부 이 파일에 담기고, 빌더 3종이 이걸 읽습니다.
- **기하는 100% 결정론(ezdxf)** — 좌표는 항상 도면에서 계산. AI는 분류(category)와 치수만 채우며, FreeCAD 코드를 생성하지 않습니다.
- 벽 인식 핵심: **interval-greedy 페어링** — 평행한 두 면선을 짝지어 정확한 중앙 센터선과 두께를 산출(1:N 면선까지 처리).

---

## 2. 빠른 시작 (GUI)

1. `run_gui.bat` 더블클릭 (또는 `python mep_gui.py`)
2. **1) File selection**에서 도면 선택 — **DXF 또는 DWG** (DWG는 무료 ODA File Converter 설치 시 자동 변환). `Layer map` / `Block map`은 기본값(`layer_map.csv`, `block_map.csv`) 그대로 두면 됨.
3. 버튼을 **왼쪽부터 순서대로**:
   - **(1) Scan drawing** — 도면 레이어/블록/엔티티 통계 확인
   - **(2) Parse -> geometry.json** — 벽/기둥/슬래브 추출 (로그에 `[QA] 면선커버 %` 자동 표시)
   - **(2b) 누락 진단** — 오버레이 PNG: 파랑=생성된 벽, **빨강=누락 의심 구간** 이 한눈에
   - **(3) 3D 미리보기(브라우저)** — 빌드 전 결과 즉시 확인 (FreeCAD 불필요)
   - **(4) IFC 빌드 (FreeCAD 불필요)** — `.ifc` 생성 → Revit/ArchiCAD/BlenderBIM에서 열기. 우측 **벽 접합(코너)** 체크 시 맞닿는 벽이 IFC 표준으로 연결돼 코너가 마이터 처리되고, 표준 물량셋(`Qto_*`)도 함께 기입됩니다
   - **(4b) 3D Build (FreeCAD)** — FreeCAD 설치 시에만 활성. `.FCStd` + `.ifc` 생성
   - **(5) 물량 Excel** — 벽(두께별 길이·면적·체적)/기둥/슬래브/창호/MEP 물량집계(BOQ)

> 처음이면 **(1)→(2)→(2b)** 로 누락을 확인하고(빨간 구간이 많으면 레이어 매핑 보강), **(3)** 미리보기로 확인 후 **(4)** 로 IFC를 뽑는 흐름을 권장합니다.

---

## 3. 설치 / 요구사항

- **Python 3.11 권장** (shapely/GEOS 휠 + PyInstaller 안정). 3.12~3.14도 동작.
- 의존 패키지:

| 용도 | 패키지 | 필수? |
|---|---|---|
| DXF 파싱 | `ezdxf` | ✅ 필수 |
| 벽 페어링/기하 | `shapely`, `numpy` | ✅ 필수 |
| IFC 빌드 (4) | `ifcopenshell` | IFC 쓸 때 |
| 3D 미리보기 (3) | (없음 — three.js 내장) | — |
| FreeCAD 빌드 (4b) | FreeCAD 1.0+ 설치 | 4b 쓸 때 |
| LLM 자동분류 | `anthropic` + `ANTHROPIC_API_KEY` | 선택 |
| Vision 폴백 | `matplotlib` | 선택(실험) |

설치:
```bash
pip install ezdxf shapely numpy ifcopenshell
# 선택: pip install anthropic matplotlib
```

> FreeCAD가 없어도 **(1)(2)(3)(4) 전부** 사용 가능합니다. (4b)만 FreeCAD가 필요합니다.

---

## 4. GUI 상세

**파일 행**
- `DXF drawing` : 입력 도면 (`.dxf` / `.dwg`). DWG는 무료 [ODA File Converter](https://www.opendesign.com/guestfiles/oda_file_converter) 설치 시 자동 변환됩니다(변환본은 DWG 옆에 캐시). 미설치면 안내가 뜨며, CAD에서 직접 DXF로 내보내도 됩니다(**DWG->DXF checklist** 버튼 참고).
- `Layer map` : 레이어명 → 요소 분류 규칙. **Edit** 버튼으로 GUI 편집 가능.
- `Block map` : 블록(INSERT)명 → 요소 분류 규칙.

**체크박스**
- `LLM` : 미매핑 레이어/블록을 Claude로 자동분류(신뢰도>0.8 자동적용). `ANTHROPIC_API_KEY` 있으면 기본 켜짐.
- `Vision` : 분류 실패분을 도면 렌더 후 Claude Vision으로 영역분류(실험적, 기본 꺼짐).

**검토 트리**
- (2) Parse 후 `needs_review` 항목과 미매핑 레이어 제안이 표시됩니다. 여기서 분류를 바로잡아 `layer_map.csv`에 반영하면 재현성이 보장됩니다.

---

## 5. 레이어 / 블록 매핑 (정확도의 핵심)

`layer_map.csv` 형식:

```csv
pattern,category,width,height,thickness
WALL|벽|CON,wall,200,2800,
COL|기둥|Block_C,column,,3000,
SLAB|FLOOR|바닥|슬래브,slab,,,200
DOOR|WIND|문|창,opening,,,
A\-STEEL,wall,,,
```

- `pattern` : **정규식**(대소문자 무시). `|`로 여러 별칭. 특수문자는 `\` 이스케이프(예: `A\-STEEL`).
- `category` : `wall` `column` `slab` `zone` `opening` `pipe` `duct` `tray` 중 하나.
- `width` / `height` / `thickness` : mm. 비우면 도면에서 자동 검출하거나 기본값 사용.
  - 벽: `width`=두께(검출 실패 시 폴백), `height`=벽 높이
  - 기둥: `height`=기둥 높이 (단면은 도면/블록에서 검출)
  - 슬래브: `thickness`=슬래브 두께

`block_map.csv`도 같은 형식이며 **블록명**에 매칭됩니다. 기둥·문·창은 보통 블록(INSERT)이라 여기서 분류합니다.

> **팁**: (1) Scan으로 미매핑 레이어/블록명을 확인 → `layer_map.csv`에 패턴 추가 → 재파싱. 이 방식이 per-요소 수정보다 **결정론적·재현 가능**합니다.

---

## 6. 빌드 옵션: IFC vs FreeCAD

| | **(4) IFC 빌드** | **(4b) FreeCAD 빌드** |
|---|---|---|
| 엔진 | `ifc_builder.py` (IfcOpenShell) | `freecad_builder.py` (FreeCAD Arch) |
| FreeCAD 필요 | ❌ 불필요 | ✅ 설치 필요 |
| 출력 | `.ifc` | `.FCStd` + `.ifc` |
| 열기 | Revit / ArchiCAD / BlenderBIM / 미리보기 | FreeCAD |
| 안정성 | 크래시·한글경로 문제 없음 (권장) | FreeCAD 버전 의존 |

대부분의 경우 **(4) IFC 빌드를 권장**합니다 — FreeCAD의 크래시/한글경로/recompute 문제를 전부 회피합니다.

---

## 7. CLI 사용법

GUI 없이 터미널에서:

```bash
# 파싱: DXF → geometry.json
python dxf_parser.py "지하3층.dxf" -m layer_map.csv -b block_map.csv -o geometry.json

# 도면 통계만
python dxf_parser.py "지하3층.dxf" --scan

# DWG→DXF 내보내기 체크리스트
python dxf_parser.py --checklist

# AI 자동분류 + 고신뢰 제안을 layer_map.csv에 자동 추가
python dxf_parser.py "지하3층.dxf" -m layer_map.csv --llm --auto-map

# IFC 빌드: geometry.json → .ifc
python ifc_builder.py geometry.json out.ifc --storey B3 --z 0

# 다층 스태킹: 층별 geometry 를 한 IFC 로 (z 생략 시 벽 높이로 자동 누적)
# floors.json = [{"geometry": "b3.geometry.json", "storey": "B3", "z": 0},
#                {"geometry": "b2.geometry.json", "storey": "B2"}]
python ifc_builder.py --project floors.json building.ifc

# 벽 코너를 IFC 표준으로 접합(마이터 자동 생성) + 표준 물량셋 기입
python ifc_builder.py geometry.json out.ifc --connect

# 누락 진단 오버레이 PNG (파랑=생성 벽, 빨강=누락 의심)
python diag_overlay.py geometry.json

# 물량집계(BOQ) Excel — 벽/기둥/슬래브/창호/MEP (--csv: 단가 연결용 CSV 추가)
python boq_export.py geometry.json --csv

# 통심선(구조 그리드) 검출 + 기둥 위치 검증
python grid_detect.py plan.dxf -g geometry.json

# DWG → DXF 변환 (LibreDWG 우선, ODA 폴백. --which 로 탐지 확인)
python dwg_converter.py plan.dwg

# 3D 미리보기 HTML (geometry.json 또는 DXF 직접)
python preview.py geometry.json
python preview.py "지하3층.dxf" -m layer_map.csv -b block_map.csv
```

**`dxf_parser.py` 주요 옵션**

| 옵션 | 설명 |
|---|---|
| `-m, --map` | layer_map.csv (없으면 내장 기본규칙) |
| `-b, --blockmap` | block_map.csv |
| `-o, --out` | 출력 경로 (기본 `geometry.json`) |
| `--scan` | 인벤토리 통계만 출력 |
| `--checklist` | DWG→DXF 체크리스트 |
| `--llm` | 텍스트 AI 분류 + 자동적용 (`ANTHROPIC_API_KEY` 필요) |
| `--vision` | Vision 폴백 (실험적) |
| `--ai-threshold` | AI 자동적용 신뢰도 임계값 (기본 0.8) |
| `--auto-map` | 신뢰도 높은 제안을 layer_map.csv에 자동 추가 |
| `--edits` | preview.py에서 받은 EID 수정 사이드카(edits.json) 적용 |

---

## 8. 3D 미리보기 (브라우저)

`preview.py`는 FreeCAD 없이 `geometry.json`(또는 DXF)을 **오프라인 HTML**(three.js 내장)로 렌더합니다. 빌드 전에 벽/기둥/슬래브가 제대로 잡혔는지 즉시 확인하는 용도입니다. `--no-open`으로 자동 열기를 끌 수 있습니다.

---

## 9. MCP 서버 (Claude / Gemini 연동)

`mep_mcp_server.py`는 결정론 엔진을 MCP 도구 7종으로 노출해, Claude Desktop·Claude Code·Gemini CLI에서 대화로 파싱→검토→진단→빌드를 지휘할 수 있게 합니다.

**도구**: `parse_dxf`(DWG 자동변환 포함) · `get_review_items` · `update_geometry_overrides` · `change_category` · `apply_layer_rule` · **`diagnose_build`**(커버리지·레이어별 누락 진단 — AI가 결과를 보고 스스로 수정하는 루프) · `build_freecad`

**Claude Desktop 설정** (`claude_desktop_config.json`):
```json
{
  "mcpServers": {
    "mep-parser": {
      "command": "C:\\Program Files\\Python314\\python.exe",
      "args": ["c:\\AI program\\3D Modeling\\mep-parser\\mep_mcp_server.py"]
    }
  }
}
```
설정 변경/코드 수정 후에는 **Claude Desktop을 완전히 종료 후 재시작**해야 서버가 다시 로드됩니다. (자세한 내용은 `MCP_README.md`)

> 원칙: MCP를 통해서도 AI는 분류·치수만 제안하고 좌표는 엔진이 계산합니다(결정론 유지).

---

## 10. 현장 PC 배포 (.exe)

Python 없는 현장 PC용 단일 실행파일:
```bash
build_exe.bat          # → dist\MEP-Parser.exe
dist\MEP-Parser.exe --selftest   # 스모크 테스트
```
생성된 `.exe`는 더블클릭으로 GUI가 뜹니다 (Python 설치 불필요).

---

## 11. 동작 원리 (왜 정확한가)

- **외곽선 → 센터선**: DXF는 벽을 두 평행 면선으로 그립니다. 엔진은 평행쌍을 찾아 **두 면선의 정확히 중앙**을 센터선으로, 면선 간격을 두께로 산출합니다.
- **interval-greedy 페어링**: 면선을 거리순으로 짝짓되 세그먼트 *전체*가 아니라 *겹침 구간*만 점유합니다. 덕분에 "긴 면선 1개 ↔ 짧은 면선 다수(1:N)"도 구간별로 올바른 파트너와 매칭됩니다. (실측 기준 면선 커버리지 56%→95%, 거짓 중복 없음)
- **결정론 보장**: 같은 도면 + 같은 매핑 = 항상 같은 결과. AI 결과는 시그니처 해시 캐시로 재현성을 보강합니다.

---

## 12. 문제 해결

| 증상 | 원인 / 해결 |
|---|---|
| GUI 실행 시 한글 깨짐/`'툝?대┃'` | `run_gui.bat`로 실행(UTF-8 설정 포함). |
| **(4b) 3D Build 버튼 비활성** | FreeCAD 미설치/미탐지. **(4) IFC 빌드**를 사용하거나 FreeCAD 설치. |
| 벽이 누락/엉뚱한 위치 | **(2b) 누락 진단**으로 빨간 구간의 레이어 확인 → `layer_map.csv`에 추가 후 재파싱. (미커버가 특정 레이어에 몰려 있으면 그 레이어가 미매핑) |
| `needs_review` 많음 | 짝 못 찾은 단독 면선(`single_offset`). (2b) 진단·미리보기로 확인, 필요 시 레이어 매핑 보강. |
| IFC 빌드 시 `ifcopenshell` 오류 | `pip install ifcopenshell numpy`. |
| MCP `failed / Server disconnected` | 설정의 Python에 `mcp`·`ezdxf` 설치 확인. 수정 후 **Claude Desktop 재시작**. |
| DWG 파일이 변환 안 됨 | 무료 **ODA File Converter** 설치 필요(안내 다이얼로그의 링크). 대안: CAD에서 DXF로 내보내기(`--checklist` 참고). |
| 물량 Excel 저장 실패 | 같은 파일이 Excel에서 열려 있으면 닫고 다시 시도. |

---

## 부록: 파일 구성

| 파일 | 역할 |
|---|---|
| `mep_gui.py` | GUI (메인 진입점) |
| `dxf_parser.py` | DXF → geometry.json (결정론 추출 엔진 + QA 자기검증) |
| `ifc_builder.py` | geometry.json → IFC (FreeCAD 불필요, `--project` 다층) |
| `freecad_builder.py` | geometry.json → FreeCAD (.FCStd) |
| `preview.py` | geometry.json/DXF → 브라우저 3D HTML |
| `diag_overlay.py` | 누락 진단 오버레이 PNG |
| `boq_export.py` | 물량집계(BOQ) Excel + 단가연결 CSV |
| `grid_detect.py` | 통심선 검출 + 기둥 위치 검증 |
| `dwg_converter.py` | DWG → DXF 자동 변환 (LibreDWG / ODA) |
| `mep_mcp_server.py` | MCP 서버 (Claude/Gemini 연동, 도구 7종) |
| `layer_map.csv` / `block_map.csv` | 레이어/블록 분류 규칙 |
| `run_gui.bat` | GUI 실행 (UTF-8) |
| `build_exe.bat` / `mep_parser.spec` | 단일 .exe 빌드 |
| `MCP_README.md` | MCP 서버 상세 |
