# FreeCAD 라이브 자연어 모델링 — 사용법

FreeCAD에서 **DXF를 보면서 객체를 클릭하고, Claude에게 자연어로 명령**해 벽·창문을 만든다.
("여기 선들로 벽 세워줘" / "이 벽에 창문 넣어줘") Claude는 화면 스크린샷으로 결과를 확인한다.

## 구조
```
사용자(FreeCAD에서 DXF 보며 객체 클릭)
        ↕ FreeCAD 3D뷰로 직접 확인 + Claude는 get_vision_screenshot로 확인
Claude Desktop ──MCP──▶ mcp_freecad_live.py ──HTTP 8082──▶ freecad_live_addon.py (FreeCAD 내부)
                                                              └▶ extractors / freecad_builder 재사용
```
- 결정론 원칙 유지: 좌표·형상은 엔진이 계산, Claude는 **고수준 도구만** 호출(코드 생성 안 함).
- 라이브 작업은 단일 요소라 빠름(대형 빌드 타임아웃 없음). 창문은 파라메트릭 Part::Cut → recompute 후에도 유지.

## 1회 설정
1. `pip install mcp ezdxf shapely` (FreeCAD 파이썬과 Claude 실행 파이썬 양쪽)
2. `claude_desktop_config.json` 내용을 `%APPDATA%\Claude\claude_desktop_config.json` 에 병합
   (이미 `mep-parser` + `freecad-live` 두 서버 등록됨) → Claude Desktop 재시작.

## 매번 실행 순서
1. **FreeCAD 실행** → 매크로 콘솔/파이썬 콘솔에서 라이브 서버 기동:
   ```python
   exec(open(r"c:/AI program/3D Modeling/mep-parser/freecad_live_addon.py", encoding="utf-8").read())
   ```
   → 콘솔에 `[Live Add-on] Listening on http://127.0.0.1:8082` 뜨면 준비 완료.
   (포트 점유 에러 시 FreeCAD 완전 종료 후 재시작)
2. **DXF 가져오기** — Claude에게 "이 DXF 열어줘"(import_dxf_live) 하거나 FreeCAD에서 직접 import.
3. **클릭 + 명령** — 아래 워크플로.

## 워크플로 (벽 + 창문)
### 벽 세우기
1. FreeCAD에서 벽이 될 **DXF 선들을 드래그/클릭 선택**.
2. Claude에게: **"선택한 선들로 벽 세워줘, 높이 3미터"**
   → Claude: `get_selection`(role=dxf_lines 확인) → `make_wall_from_selection(height=3000)` →
     `get_vision_screenshot`로 결과 확인.

### 창문 넣기
1. FreeCAD에서 **벽을 클릭**(창 넣을 지점을 정확히 클릭하면 그 위치에 배치됨).
2. Claude에게: **"이 벽에 폭 1.2m 높이 1.5m 창문 넣어줘"**
   → Claude: `get_selection`(role=wall 확인) → `add_window_to_wall(width=1200, height=1500)` →
     스크린샷 확인.
   - 위치: 클릭 지점 우선 → 없으면 `position`(0~1 또는 mm) → 없으면 벽 중앙.
   - 문: `subtype="door"`(sill 0, 바닥부터). 창: 기본 sill 900.

## 라이브 MCP 도구 (mcp_freecad_live.py)
| 도구 | 역할 |
|------|------|
| `check_connection` | 라이브 서버 연결 확인 |
| `import_dxf_live(path)` | DXF를 현재 문서에 import |
| `get_selection()` | **현재 클릭·선택한 객체**(role/centerline/picked_point) 반환 |
| `make_wall_from_selection(height, width, columns)` | **선택한 DXF선 → 벽(+기둥)** |
| `add_window_to_wall(width, height, sill, subtype, position, wall_label)` | **선택한 벽에 창/문** |
| `auto_detect_walls(path)` | 도면 전체 자동 파싱→라이브 빌드(배치) |
| `make_wall_live(x1,y1,x2,y2,...)` | 좌표 지정 단일 벽 |
| `get_vision_screenshot(fit_all)` | 3D 뷰 캡처(AI 시각 확인) |

## 한계 / 후속(v1.1)
- 기둥/문/슬래브 전용 자연어 명령, 창호일람 기반 크기 자동선택, 이동/삭제/수정 명령은 후속.
- `get_selection`·창문 삽입의 GUI 동작(Gui.Selection)은 라이브 FreeCAD에서만 작동(헤드리스 불가).
- 핵심 헬퍼(벽 빌드·창 void)는 freecadcmd 헤드리스 검증 완료(파라메트릭 유지 확인).
