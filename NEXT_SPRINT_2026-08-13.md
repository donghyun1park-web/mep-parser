# Sprint C — A부 마감 및 판정 일관성 복구

**작성일:** 2026-08-13
**전제 문서:** `IMPROVEMENT_PLAN_2026-08.md` (A부/B부 실행계획). 이 문서는 그 계획을 대체하지 않고,
**A부를 닫기 전에 반드시 해결해야 할 일관성 결함**과 그 다음 마일스톤만 다룬다.

## 왜 이 스프린트가 먼저인가

A0~A5는 각각 잘 만들어졌지만, **모듈 사이에서 "완료"와 "믿을 수 있음"의 정의가 서로 다르다.**
그 결과 수정 검증에 성공한 케이스조차 사용자에게는 "평가 불가"로 보인다. 이 상태로 A3(MPI)이나
B부로 넘어가면, 새로 만드는 게이트가 무엇을 통과시키는지 아무도 확신할 수 없다.

## 검증된 현재 상태 (2026-08-13 실측, 주장 아님)

| 항목 | 실측값 | 판단 |
|---|---|---|
| 전체 pytest | **8 failed, 561 passed, 14 skipped** | 계획서의 "366 passed"는 낡음. 기준선이 빨간불 |
| 실패 위치 | `tests/test_cfd_post.py::NumericalSensitivityPostprocessTests` 8건 | A5 TDD Step 1만 완료(구현 전) — 회귀 아님 |
| 핵심 모듈 회귀 | energy_balance·advice·auto_extend·result_gate·case_cache·parallel **102 passed** | 정상 |
| 개구부 preflight | `opening_preflight.v2`, 단말 30개 중 이탈 2건 검출, `jet_metrics_citable=false` | 정상 작동 |
| 결과 게이트 | 3단계 분리 실동작 확인 | 정상 작동 |
| 수정 검증 케이스 2종 | 폐합 103%/106% 인데 `citation_status=NOT_EVALUATED`, `blockers=[screening_engine, residuals]` | **모순 — C1 대상** |
| 자동연장 종료조건 | `cfd_run.py` `if closure_ready: return` (폐합만) | **게이트와 불일치 — C1 대상** |
| 잔차 기준 | fvSolution `1e-5` / `cfd_report.CONVERGENCE_TARGETS` `1e-5` / `cfd_result_gate.RESIDUAL_LIMITS` `T 1e-2` | **3중 정의, 게이트가 최다 느슨 — C2 대상** |
| SGI 전체 ΔT | 이론 대비 **+5.94%** (±5% 미달) | C2에서 물리 기준과 연결 |

---

## Task C0 — 초록 기준선 복구 (최우선)

**문제:** 테스트가 빨간 상태로 유지되면 새 회귀와 기존 미구현을 구분할 수 없다.
지금은 8건이 A5 미구현이라는 것을 사람이 기억해야만 판독이 가능하다.

**Files:** `cfd_post.py`, `tests/test_cfd_post.py`

- [ ] **Step 1: 다음 중 하나를 선택해 기준선을 초록으로 만든다.**
  - (a) `cfd_post`에 `compute_occupied_volume_qois_from_vtu`, `read_trusted_exhaust_temperature_rise_qoi`,
        `PostprocessEvidenceError`를 구현한다(A5.2 선행 조건과 동일 범위).
  - (b) 구현 전까지 해당 8건에 `@unittest.skip("A5.2 미구현 — 구현 시 해제")`를 명시적으로 단다.
  - 임의로 테스트를 삭제하거나 assert를 약화하지 않는다.
- [ ] **Step 2: CI 산출물에 전체 결과와 skip 사유를 남긴다.**

**수용 기준:** `python -m pytest tests/ -q`가 **failed 0**으로 끝난다. skip이 있다면 각 skip의 사유가
"어떤 작업이 끝나면 해제되는지"까지 문자열에 적혀 있다.

**주의:** (b)를 고르면 A5.2 착수 시 skip 해제를 잊지 않도록 `IMPROVEMENT_PLAN`의 A5.2 Step에
"skip 해제"를 명시적 체크박스로 추가한다.

---

## Task C1 — "완료"의 정의를 하나로 통일 (핵심) — ✅ 재조사 결과 이미 통일되어 있었음

**정정 (2026-08-13 코드 확인):** 이전 판단("자동연장은 폐합만 보고 멈춘다")은 **틀렸다.**
`cfd_run.py`의 `_auto_extend_convergence()`를 직접 읽어 보니 `cfd_result_gate.RESIDUAL_LIMITS`,
`LEGACY_SCREENING_RESIDUALS`, `CLOSURE_OK`를 그대로 import해서 쓰고 있었다 — 즉 자동연장의
`closure_ready`와 게이트의 `convergence_status`는 **이미 같은 함수·같은 상수**를 쓴다.

이전 판단은 종료 로그 문구("에너지 폐합 통과 — 종료")만 보고 내린 추측이었고, 실제 호출부
(`_auto_extend_convergence`)를 읽지 않은 채 결론 내린 것이었다. `SGI 전체 로비 수정검증` 케이스를
`cfd_run.closure_status()`로 재계산한 결과:

```
closure_pct: 105.94 (정상)
closure_ready: False
auto_extend_blockers: ["residuals"]
n_iters: 1000  (controlDict endTime도 1000 그대로)
Uy: 1.38e-3 (한계 1e-3 초과)   Uz: 1.03e-3 (한계 1e-3 초과)
need_iters: 23317 (감쇠 예측)
```

즉 이 케이스는 `run_until_closed()`(자동연장)를 거치지 않고 **고정 1000회 단발 실행**으로
멈춘 것이었다 — endTime이 연장된 흔적이 없다. 자동연장이었다면 `need_iters=23317`을 보고
계속 늘렸을 것이다. 게이트가 `NOT_EVALUATED`를 낸 것은 **정확한 판정**이다: 아직 진짜로
안 끝났다. "계산 완료인데 평가 불가"라는 모순은 애초에 없었다.

**남은 진짜 질문 (버그 아님, 설계/운영 판단 필요):**
1. Studio GUI의 기본 "실행" 버튼이 `run_case()`(고정 반복)를 쓰는지 `run_until_closed()`
   (자동연장)를 쓰는지 확인이 필요하다. 전자라면, 사용자가 별도로 "자동연장"을 고르지 않는 한
   결과가 항상 이렇게 미완 상태로 끝날 수 있다 — 이건 UX 문제이지 판정 로직 버그가 아니다.
2. `need_iters=23317`은 매우 크다. Ux/Uy/Uz 1e-3 임계값이 현실적인지, 아니면 C2에서
   ΔT 정확도 실측으로 더 이른 반복에서 이미 충분한지 확인할 가치가 있다.

**조치:** 이 Task는 코드 변경 없이 종료. Studio 실행 버튼이 무엇을 호출하는지는 C3(실사용 DXF
완주) 과정에서 자연히 드러나므로 별도 조사하지 않는다.

---

## Task C2 — 잔차 기준 단일화 + 물리 기준과 연결 — 부분 완료 (2026-08-13)

**정정된 그림:** fvSolution의 `residualControl`은 **openings 케이스(SGI 전부 포함)에서는
`cfd_export._build_case_into()`가 통째로 제거한다** (주석: "잔차 자가종료가 폐합 전에 멈추는
것을 실측"). 즉 이 케이스들에서 fvSolution 1e-5는 애초에 작동하지 않는다. 실제로 살아있는
정의는 두 개뿐이었다: `cfd_report.CONVERGENCE_TARGETS`(예측 목표, 1e-5)와
`cfd_result_gate.RESIDUAL_LIMITS`(신뢰 판정, 1e-3~1e-2). 그리고 이 둘은 **의도적으로 다르게
설계된 2단 구조**로 보인다 — 예측 목표는 "완전 수렴"을 가리키는 참고값이고, 신뢰 판정은
에너지폐합율(`CLOSURE_OK`)·질량수지와 **함께** 작동하는 다중 방어선의 한 축이다. 158% 사고는
"느슨한 잔차 기준 하나만으로 수렴 선언"이 원인이었는데, 지금은 독립적인 폐합율 검사가 같은
실패를 잡아낸다. 그래서 두 값을 억지로 같게 만드는 것이 오히려 틀린 처방일 수 있다.

(참고: `cfd_numerics.THERMAL_RESIDUAL_LIMITS`는 별개 — body-fitted 설계검토 경로(B부)의
완전히 다른 계약(`thermal_numerics.v1`)이라 통합 대상이 아니다.)

- [x] **Step 3 (선반영): 단일 모듈에서 정의하고 두 곳이 모두 import 하게 한다.**
      `cfd_convergence_spec.py` 신설. `FORECAST_TARGET_RESIDUALS`(예측용)와
      `SCREENING_TRUST_RESIDUAL_LIMITS`(신뢰판정용)를 각각 단일 정의로 분리하고,
      `cfd_result_gate.RESIDUAL_LIMITS`와 `cfd_report.CONVERGENCE_TARGETS`는 그대로 가져다 쓴다
      (재정의 아님, `is` 동일 객체).
- [x] **Step 4: 회귀 테스트 추가.** `tests/test_cfd_convergence_spec.py` —
      두 모듈이 실제로 같은 객체를 참조하는지(`assertIs`), 그리고 예측 목표가 신뢰 임계값보다
      느슨해지지 않는지(`forecast <= trust`, 모순 방지)를 고정한다. 569 passed로 확인.
- [x] **Step 1a: 기존 스냅샷(0~1000, 추가 실행 없음)으로 1차 확인 — 완료 (2026-08-13).**
      `SGI 전체 로비EHP 3대_수정검증` 케이스의 이미 저장된 8개 time snapshot(0,100,500~1000)에서
      각 시점의 잔차와 ΔT 오차(이론 평형 대비)를 직접 계산했다(스크립트:
      `scratchpad/c2_residual_vs_dt.py`, 결과: `scratchpad/c2_residual_vs_dt.json`).

      | iter | Uy 잔차 | Uz 잔차 | ΔT 오차 |
      |---|---|---|---|
      | 500 | 2.14e-3 | 1.42e-3 | 3.87% |
      | 700 | 1.84e-3 | 1.10e-3 | 5.62% |
      | 800 | 1.68e-3 | 1.05e-3 | **6.00%** |
      | 900 | 1.50e-3 | 1.05e-3 | **5.96%** |
      | 1000 | 1.38e-3 | 1.03e-3 | **5.86%** |

      **잔차는 계속 줄어드는데(2.14e-3→1.38e-3) ΔT 오차는 800회 이후 정체한다(6.00→5.96→5.86%).**
      이는 남은 격차가 반복 부족이 아니라 다른 원인(격자 해상도·수치기법·개구부 스냅 왜곡 등)일
      가능성을 시사한다 — 맞다면 잔차가 현재 임계값(1e-3)에 도달할 때까지(예측 23,317회, ~35시간
      직렬) 기다려도 정확도가 개선되지 않을 수 있다.
- [x] **Step 1b: 확인 실행 — 완료 (2026-08-13, 08:27~10:38, 직렬 약 2h11m).**
      endTime 1000→2500(1500회 추가)로 이어 돌렸다. 결과(`scratchpad/c2_residual_vs_dt.json`):

      | iter | Ux | Uy | Uz | ΔT 오차 |
      |---|---|---|---|---|
      | 1100 | 7.3e-4 | 1.28e-3 | 1.03e-3 | 5.84% |
      | 1800 | 5.7e-4 | 1.06e-3 | 8.8e-4 | 6.69% |
      | 2100 | 5.3e-4 | **9.3e-4** | **7.4e-4** | 6.21% |
      | 2300 | 5.1e-4 | 9.7e-4 | 6.4e-4 | 5.79% |
      | 2500 | 4.9e-4 | 9.2e-4 | 6.0e-4 | 5.58% |

      **결론 — 두 가설 모두 부분적으로 맞고, 실제로 중요한 답은 셋째다:**
      1. **잔차는 iter ~2100에서 이미 현재 임계값(Ux/Uy/Uz=1e-3)을 통과했다** — 예측했던
         23,317회가 아니라 **약 1,100~1,500회만 더**로 충분했다. `residual_decay_forecast()`의
         로그선형 외삽이 이 케이스에서 과도하게 비관적이었다(후속 개선 후보, 긴급하지 않음).
      2. **ΔT 오차는 그래도 정체한다** — 잔차가 임계값을 통과한 뒤(2100~2500)도 5.6~6.7% 사이를
         오갈 뿐 0%로 수렴하지 않는다. 이 격차는 반복 부족이 아니라 다른 원인(수치기법·격자·
         개구부 스냅 왜곡 등)이며, **`SCREENING_ONLY` 등급이 애초에 약속하는 정확도가 딱 이
         정도**라는 뜻이다. `SCREENING_ONLY`는 "설계 승인 정확도"가 아니라 "솔버가 물리적으로
         타당한 상태에 정착했다"는 의미이므로, 이 정체 자체는 버그가 아니다.
      3. **따라서 `SCREENING_TRUST_RESIDUAL_LIMITS`를 완화할 근거는 없다.** 느슨하게 만들 필요
         없이 현재 값 그대로 도달 가능했다. 문제는 임계값이 아니라 **이 케이스가 애초에
         `run_until_closed()`(자동연장)를 거치지 않고 1000회에서 멈췄다는 것**이었다(C1 조사
         결과와 일치).
      - **검증**: 확장 후 `cfd_report.case_summary()`를 재실행하니
        `convergence_status: PASS`, `citation_status: SCREENING_ONLY`,
        `blockers: ["screening_engine"]`(정상 라벨, 실제 차단 아님)로 바뀌었다.
        `NOT_EVALUATED`였던 케이스가 실측으로 정당하게 `SCREENING_ONLY/citable=True`가 됐다.

**Task C2 최종 상태: 완료.** 잔차 기준은 단일 소스로 통합됐고(Step 3-4), 물리적 근거 실측(Step 1)
결과 현재 값을 유지하는 것이 맞다는 결론을 얻었다. 값 변경은 하지 않는다.
- [ ] **Step 2: Step 1 결과로 `SCREENING_TRUST_RESIDUAL_LIMITS` 값을 조정하고(필요시), 각 값
      옆에 실측 근거를 주석으로 남긴다.**
- [ ] **Step 5: 필요 없어짐** — 스크리닝/설계검토 분리는 이미 `cfd_numerics.THERMAL_RESIDUAL_LIMITS`로
      되어 있었다.

**수용 기준(갱신):** 잔차 기준이 코드 전체에서 두 곳(예측/신뢰)에만, 각각 한 곳에만 정의된다 — ✅ 완료.
느슨한 신뢰 임계값이 실측 ΔT 정확도로 뒷받침되는지는 미확인 — Step 1 실험 필요.

---

## Task C3 — 실사용 DXF 1건 완주 (다음 마일스톤)

**왜:** 코드가 15,146 → 25,342줄(+67%)로 늘었고 계약·매니페스트·스키마가 계속 추가된다.
**검증 인프라가 제품 가치를 앞지를 위험**이 있다. 다음 마일스톤은 새 계약이 아니라
**"실제 도면 한 건이 처음부터 끝까지 통과하는 것"**이어야 한다.

**전제:** C0·C1·C2 완료. 그래야 통과/차단 판정을 신뢰할 수 있다.

- [ ] **Step 1: 실제 현장 DXF 1건을 선택한다.** (전기실 또는 로비 — 장비일람표가 함께 있는 것)
- [ ] **Step 2: DXF → 공간·개구부·장비 확인 → 케이스 생성 → 실행 → 리포트까지 GUI만으로 수행한다.**
      중간에 명령행이나 수동 JSON 편집이 필요하면 그 지점을 결함으로 기록한다.
- [ ] **Step 3: 각 단계에서 막히면 원인·영향·다음 조치가 한국어로 표시되는지 확인한다.**
- [ ] **Step 4: 최종 결과가 `SCREENING_ONLY`로 정직하게 표시되고, 차단 사유가 실행 가능한지 확인한다.**
- [ ] **Step 5: 소요 시간과 막힌 지점을 기록해 다음 우선순위의 근거로 삼는다.**

**수용 기준:** 기계설비 담당자가 GUI만으로 DXF에서 리포트까지 도달한다. 결과가 설계 승인이
아니어도 되지만, **"무엇을 알 수 있고 무엇은 알 수 없는지"가 리포트에 명확해야 한다.**

---

## 이번 스프린트에서 하지 않을 것

- **A3 MPI 병렬**: `mpirun -np 2 hostname`조차 랭크를 못 만든다(실측, `MPI_RANK_SPAWN_HANG`).
  이는 코드가 아니라 WSL Open MPI 런타임 문제이므로, C0~C3와 병행하지 말고 **독립 조사**로 분리한다.
  조사 순서 제안: WSL 재시작 → Open MPI 재설치 → `--mca plm isolated` 등 launcher 우회 →
  MPICH 대체 검토. 어느 것도 되지 않으면 **직렬 최적화**(스킴·완화계수·초기장)로 목표를 바꾼다.
- **B부(복사·열쾌적)**: 현재 상태 유지가 옳다. B1이 "스위치 없는 계약+벤치마크"인 것은 좋은 규율이다.
- **새 계약·매니페스트 추가**: C3가 끝날 때까지 동결한다.

---

## 완료 판정

이번 스프린트는 아래가 모두 참일 때 끝난다.

1. `pytest` **failed 0** (skip은 사유·해제조건 명시)
2. 자동연장이 멈춘 케이스와 게이트 판정이 **모순되지 않음**
3. 잔차 기준이 **한 곳에서만** 정의되고 물리적 근거가 있음
4. 실제 DXF 1건이 GUI만으로 리포트까지 도달

## 우선순위 근거

C0·C1·C2는 합쳐서 하루 규모이며, **이걸 끝내야 이후 모든 작업의 통과 여부를 신뢰할 수 있다.**
특히 C1을 미루면 사용자는 계속 "돌렸는데 평가 불가"를 보게 되고, 이는 제품 신뢰를 직접 훼손한다.
C3는 그 위에서 처음으로 **제품이 실제로 쓸 만한지**를 묻는 단계다.
