# MEP-Parser 개발계획서

> 도면과 모델을 일치시키는 다섯 가지 작업
> 기준 커밋 `a4ff731` · 브랜치 `feature/cfd-pipeline` · 2026-08-25
> 읽기 좋은 버전: https://claude.ai/code/artifact/879b8297-ca47-4613-a748-be403a5ceac8

모든 수치는 `지하3층 건축평면도.dxf` 실측값이며 추정치는 포함하지 않는다.

---

## 1. 기준선 (이번 세션 확보분)

| 지표 | 값 | 비고 |
|---|---|---|
| 면선 커버리지 | **95.4%** | interval-greedy 도입 전 56% |
| 미해결 single | **0** | paired 496 · single_offset 143 · closed 38 |
| IFC 빌드 | 벽 **679** · 기둥 93 · 슬래브 8 · 스킵 0 | 707KB |
| 물량 집계 | 벽 1,558.1m · 4,362.9㎡ · **951.05㎥** | 기둥 93개 133.92㎥ · 창호 288 |

커밋: `a5effc3`(자기검증 QA) · `ee3d613`(DWG) · `5db155a`(BOQ) · `2c483e0`(다층) · `a4ff731`(문서)

남은 문제는 *얼마나 많이 잡느냐*가 아니라 *잡은 것이 맞느냐*다.

---

## 2. 조사 결과

### 2.1 미러링된 블록의 좌표가 뒤집힌다 — 실측 확인된 현존 결함

`insert_to_records`가 `virtual_entities()` 결과를 그대로 쓰는데, ezdxf는 OCS를 자동으로 WCS로
변환하지 않는다. extrusion이 `(0,0,-1)`이면 **X좌표 부호가 반전**된다.

```
지하3층 스캔
  extrusion ≠ (0,0,1)   76개  · LWPOLYLINE 62 · ARC 9 · INSERT 4 · TEXT 1
  음수 스케일 INSERT    14개  · (-1,1,1)×10 · (-1,1,-1)×4
```

### 2.2 벽 접합은 IFC 표준 API가 이미 한다 — 로컬 테스트 통과

`snap_wall_corners`·`heal_wall_junctions`로 직접 계산해 왔으나 IfcOpenShell에 표준 구현이 있고
**설치된 0.8.5에 전부 존재**한다. L코너 2벽으로 직접 검증:

```
connect_wall  → IfcRelConnectsPathElements(… .ATSTART., .ATEND.)
regenerate    → W1 고유 X = [0.0, 4.8, 5.0]
                끝단이 파트너 두께 0.2 만큼 사선 절단됨 ✓
```

기하학적 이득(마이터 코너) + 의미론적 이득(Revit·ArchiCAD가 읽는 벽 연결 관계). 현재 우리 IFC의
벽들은 서로 아무 관계가 없다.

### 2.3 DWG에 설치 프로그램이 필요 없다

GNU LibreDWG `dwg2dxf.exe` — win64 **11.5MB zip, 설치 프로그램 없음, 회원가입 없음**,
DWG R10~R2018. 현재 `dwg_converter.py`가 요구하는 ODA File Converter 설치를 대체한다.

### 2.4 물량에서 단가까지 표준 경로가 있다

buildingSMART 표준 물량셋(`Qto_WallBaseQuantities` 등)을 IFC에 심고, `ifc5d`가 XLSX·CSV로
내보내며 **Schedule of Rates(단가표)를 붙여 가격이 매겨진 내역서**를 만든다. 보유 단가 DB와
직결되는 지점.

### 2.5 우리에게 없는 것 — 그리드 검출

자매 프로젝트 2D23D(MIT)는 `S-GRID` 계열 레이어에서 통심선을 뽑아 **교차점에 기둥을 자동 배치**한다.
우리는 그리드를 전혀 보지 않는다.

---

## 3. 실행 계획 (우선순위 순)

### P1. OCS → WCS 좌표 정합 · 버그 수정

미러링된 블록의 기둥·문·창이 반대편에 생성되고 있다. 다른 모든 개선의 신뢰도가 여기 걸려 있다.

- **대상**: `dxf_parser.insert_to_records` · `entity_to_record`
- **방법**: extrusion이 `(0,0,1)`이 아닌 엔티티에 `e.ocs().to_wcs(pt)` 적용.
  INSERT는 `mcount > 1`이면 `multi_insert()`로 전개.
- **검증**: 수정 전후 76개 엔티티 좌표 변화 로그 · 진단 오버레이 육안 확인 ·
  샘플 4종 회귀 · 커버리지가 95.4%에서 떨어지지 않을 것

### P2. LibreDWG 백엔드 추가 · 진입 장벽 제거

- **대상**: `dwg_converter.py`
- **방법**: `find_libredwg()` 추가 → **LibreDWG 우선, ODA 폴백**. 미탐지 시 무설치 다운로드 안내.
- **검증**: DWG 변환본과 CAD 직접 내보낸 DXF의 요소 수·좌표 일치 대조
- ⚠️ **라이선스**: LibreDWG는 GPL-3.0. 별도 exe 호출은 문제없으나 `.exe` 동봉 배포 시 검토 필요.

### P3. IFC 표준 벽 접합 · PoC 통과

- **대상**: `ifc_builder.py`
- **방법**: 두께별 `IfcMaterialLayerSet`(Priority 포함) + `IfcWallType` → `create_2pt_wall`
  → 끝점 맞닿는 벽쌍에 `connect_wall` → `regenerate_wall_representation`
- **재사용**: 접합쌍 판정은 기존 `heal_wall_junctions`의 끝점 근접 판정 로직 활용
- **검증**: 679벽 전체 빌드 성공률·소요시간 · 코너 단면 확인 ·
  기존 `add_wall_representation` 경로를 폴백으로 유지

### P4. 표준 물량셋과 단가 연결 · 견적 연결

- **대상**: `ifc_builder.py` · `boq_export.py`
- **방법**: 빌드 시 `add_qto`/`edit_qto`로 `Qto_WallBaseQuantities` 기입 →
  `ifc5d` Schedule of Rates CSV 출력 옵션 → 보유 단가 DB 연결
- **검증**: 우리 집계값과 `ifcedit quantify` 산출값 교차 대조 (벽 1,558.1m / 951.05㎥ 기준)

### P5. 그리드 검출과 데이터 검증 · 정확도 보강

- **대상**: 신규 `grid_detect.py` · `ifctester` 규격 파일
- **방법**: 통심선 레이어에서 직교 그리드 추출 → 교차점 대비 기둥 위치 검증(누락·오배치 리포트).
  IDS로 "모든 벽에 두께", "모든 요소에 층 지정" 등 데이터 완결성 검사.
- **검증**: 그리드 교차점 대비 기둥 93개 매칭률 · IDS 리포트가 실제 결손을 잡아내는지

---

## 4. 검토 후 제외

| 후보 | 제외 근거 |
|---|---|
| **topologize** (medial axis) | 실측 6회 — 센터선이 면선을 재추적(오차 3~10mm), 총길이 42% 과생성. 채워진 벽 폴리곤이 선행돼야 해 순환 모순 |
| **NetworkX 최대매칭** 벽 페어링 | 1:1 매칭이라 이미 버린 방식으로 회귀. interval-greedy가 상위 호환 |
| **딥러닝 평면도 인식** (CubiCasa5K 계열) | 래스터 이미지 대상. 벡터 DXF에는 결정론 추출이 우월 |
| **PDF·스캔 도면 벡터화** | "기하 100% 결정론" 원칙과 충돌. OCR 좌표 신뢰 불가 |
| **Electron·Tauri 재작성** | 수 주 비용에 얻는 것은 외관뿐. 현장 배포는 단일 exe로 해결됨 |
| **Revit 플러그인 방식** | Revit 종속. FreeCAD 없이 도는 현재 방향과 정반대 |

---

## 5. 리스크

- **OCS 수정이 기존 결과를 바꾼다.** 76개 엔티티 좌표가 실제로 이동하므로, 착수 전 현재 파싱 결과를
  기준선으로 저장하고 diff로 변화 지점을 전수 확인한다.
- **표준 벽 접합은 전제조건이 많다.** `regenerate_wall_representation`은 재료 레이어셋과 Axis
  컨텍스트를 요구하며, 679벽 규모 성능은 미검증. 기존 경로를 폴백으로 반드시 남긴다.
- **의존성이 늘어난다.** `ifc5d`·`ifctester` 미설치 상태. 없을 때 조용히 건너뛰는 기존 graceful
  패턴을 지킨다.
- **슬래브 8개는 오분류다.** 물량 집계가 드러낸 사실 — 실제 슬래브가 아니라 `A-SYM` 레이어의
  77×224mm 심볼. 레이어 규칙 한 줄로 정리된다.

---

## 참조 프로젝트

| 프로젝트 | 관련성 |
|---|---|
| [2D23D](https://github.com/naquib0513/2D23D) (MIT) | 구조·철학 동일한 자매 프로젝트. 그리드 검출 참조 |
| [IfcMCP](https://docs.ifcopenshell.org/ifcmcp.html) (공식) | IFC 질의·수정 MCP. 우리 MCP(생성)와 상보적 |
| [simple-ifc](https://github.com/brunopostle/simple-ifc) | quantify → BoQ → IDS 워크플로 시연 |
| [OpenConstructionERP](https://github.com/datadrivenconstruction/openconstructionerp) (AGPL) | 도면→물량→입찰 완성형. 구조 참조용 |

---

변경은 Phase당 1커밋으로 `feature/cfd-pipeline` 위에 쌓는다.
